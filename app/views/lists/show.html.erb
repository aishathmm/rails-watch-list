<h1><%= @list.name %></h1>

<ul>
    <% @list.movies.each do |movie| %>
    <li> <%= movie.title  %> | <%= movie.overview %></li>
    <% end %>
</ul>

<%= link_to "Add to bookmarks", new_list_bookmark_path(@list) %>
